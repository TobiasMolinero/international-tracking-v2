/** @type {import("prettier").Config} */
const config = {
    semi: true,          // ; al final
    singleQuote: true,   // ' en lugar de "
    tabWidth: 2,         // indentación de 2 espacios
    trailingComma: 'es5',// coma final donde sea válido
    printWidth: 100,     // largo máximo de línea
};

export default config;